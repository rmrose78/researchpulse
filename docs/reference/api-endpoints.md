# API Endpoints

GET    /health                      ✅ done
GET    /api/search/                 ✅ done
GET    /api/search/{pmid}           ✅ done
POST   /api/reading-list/           — to be built
GET    /api/reading-list/           — to be built
DELETE /api/reading-list/{pmid}     — to be built
GET    /api/search/{pmid}/summary   — Phase 3
GET    /api/trending/               — Phase 2

Note: Current Priority (in CLAUDE.md) lists these reading-list endpoints as
completed — this table wasn't reconciled against that during the CLAUDE.md
slim-down; verify actual status before relying on either.
